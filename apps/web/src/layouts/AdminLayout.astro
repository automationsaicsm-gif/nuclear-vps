---
import '../styles/global.css';

interface Props {
  title?: string;
}
const { title = 'Admin — Nuclear VPS' } = Astro.props;

const navItems = [
  { href: '/admin', label: 'Dashboard', icon: 'M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6' },
  { href: '/admin/users', label: 'Users', icon: 'M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z' },
  { href: '/admin/services', label: 'Services', icon: 'M5 12h14M5 12a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v4a2 2 0 01-2 2M5 12a2 2 0 00-2 2v4a2 2 0 002 2h14a2 2 0 002-2v-4a2 2 0 00-2-2m-2-4h.01M17 16h.01' },
  { href: '/admin/invoices', label: 'Invoices', icon: 'M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-3 7h3m-3 4h3m-6-4h.01M9 16h.01' },
  { href: '/admin/tickets', label: 'Tickets', icon: 'M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z' },
  { href: '/admin/plans', label: 'Plans', icon: 'M9 5l7 7-7 7' },
  { href: '/admin/coupons', label: 'Coupons', icon: 'M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z' },
  { href: '/admin/affiliate', label: 'Affiliates', icon: 'M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0z' },
  { href: '/admin/settings', label: 'Settings', icon: 'M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z M15 12a3 3 0 11-6 0 3 3 0 016 0z' },
];

const currentPath = Astro.url.pathname;
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{title}</title>
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
</head>
<body class="bg-base text-text-body font-body min-h-screen flex">

  <!-- Sidebar -->
  <aside class="fixed left-0 top-0 bottom-0 w-64 bg-footer border-r border-[#312E81] flex flex-col z-30">
    <div class="p-6 border-b border-[#312E81]">
      <a href="/">
        <span class="font-heading font-black text-2xl text-text-h">NUCLEAR</span><span class="font-heading font-black text-2xl text-coral">VPS</span>
      </a>
      <p class="text-text-muted text-xs mt-1 font-heading font-bold uppercase tracking-widest">Admin Panel</p>
    </div>

    <nav class="flex-1 p-4 overflow-y-auto">
      {navItems.map(({ href, label, icon }) => {
        const isActive = currentPath === href || (href !== '/admin' && currentPath.startsWith(href));
        return (
          <a
            href={href}
            class={`flex items-center gap-3 px-4 py-3 rounded-xl mb-1 font-heading font-bold text-sm transition-all duration-200 ${
              isActive
                ? 'bg-indigo-mid text-white border-l-4 border-coral'
                : 'text-text-body hover:bg-surface hover:text-coral'
            }`}
          >
            <svg class="w-5 h-5 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d={icon}/>
            </svg>
            {label}
          </a>
        );
      })}
    </nav>

    <div class="p-4 border-t border-[#312E81] space-y-2">
      <a href="/dashboard" class="flex items-center gap-3 px-4 py-2.5 text-text-muted hover:text-coral hover:bg-surface rounded-xl font-heading font-bold text-sm transition-colors">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
        </svg>
        Client Area
      </a>
      <button id="admin-logout" class="w-full flex items-center gap-3 px-4 py-2.5 text-coral hover:bg-surface rounded-xl font-heading font-bold text-sm transition-colors">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/>
        </svg>
        Logout
      </button>
    </div>
  </aside>

  <!-- Main content -->
  <div class="ml-64 flex-1 flex flex-col min-h-screen">
    <header class="bg-footer border-b border-[#312E81] px-6 py-4 flex items-center justify-between sticky top-0 z-20">
      <h1 class="font-heading font-black text-text-h text-xl">{title.replace(' — Nuclear VPS', '')}</h1>
      <div class="flex items-center gap-3">
        <span class="bg-coral/10 border border-coral/30 rounded-full px-3 py-1 text-coral text-xs font-heading font-black uppercase tracking-wider">Admin</span>
        <div class="w-9 h-9 rounded-full bg-coral flex items-center justify-center">
          <span class="font-heading font-black text-white text-sm" id="admin-initials">A</span>
        </div>
      </div>
    </header>

    <main class="flex-1 p-6 bg-base">
      <slot />
    </main>
  </div>

  <script>
    const stored = localStorage.getItem('nuclear-vps-auth');
    if (!stored) {
      window.location.href = '/login?redirect=' + encodeURIComponent(window.location.pathname);
    } else {
      try {
        const state = JSON.parse(stored);
        const user = state?.state?.user;
        if (!user || user.role !== 'ADMIN') {
          window.location.href = '/dashboard';
        } else {
          const el = document.getElementById('admin-initials');
          if (el) el.textContent = (user.firstName?.[0] || 'A') + (user.lastName?.[0] || '');
        }
      } catch {
        window.location.href = '/login';
      }
    }

    document.getElementById('admin-logout')?.addEventListener('click', async () => {
      await fetch('/api/v1/auth/logout', { method: 'POST', credentials: 'include' });
      localStorage.removeItem('nuclear-vps-auth');
      window.location.href = '/';
    });
  </script>
</body>
</html>
